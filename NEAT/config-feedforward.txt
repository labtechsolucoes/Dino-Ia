[NEAT]
fitness_criterion      = max
fitness_threshold      = 100000
pop_size               = 20
reset_on_extinction    = False
no_fitness_termination = False

[DefaultGenome]
# Ativações
activation_default      = relu
activation_mutate_rate  = 0.1
activation_options      = relu sigmoid

aggregation_default     = sum
aggregation_mutate_rate = 0.0
aggregation_options     = sum

# Pesos (Bias)
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_max_value          = 30.0
bias_min_value          = -30.0
bias_mutate_power       = 0.5
bias_mutate_rate        = 0.7
bias_replace_rate       = 0.1

# Compatibilidade Genética (Para dividir em espécies)
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient   = 0.5

# Mutações Estruturais (Adicionar/Remover Conexões)
conn_add_prob           = 0.5
conn_delete_prob        = 0.2

# Habilitar/Desabilitar conexões
enabled_default         = True
enabled_mutate_rate     = 0.01

feed_forward            = True
initial_connection      = full_direct

# Mutações Estruturais (Adicionar/Remover Neurônios)
node_add_prob           = 0.2
node_delete_prob        = 0.2

# ARQUITETURA DA REDE NEURAL (Crucial para casar com o jogo)
# 10 Entradas (Estado do Jogo com munição, bazuca etc)
num_inputs              = 10
# 4 Saídas (Correr, Pular, Abaixar, Atirar)
num_outputs             = 4
# 5 Neurônios Ocultos iniciais
num_hidden              = 5

response_init_mean      = 1.0
response_init_stdev     = 0.0
response_max_value      = 30.0
response_min_value      = -30.0
response_mutate_power   = 0.0
response_mutate_rate    = 0.0
response_replace_rate   = 0.0

# Pesos das Conexões
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_max_value        = 30.0
weight_min_value        = -30.0
weight_mutate_power     = 0.5
weight_mutate_rate      = 0.8
weight_replace_rate     = 0.1

[DefaultSpeciesSet]
compatibility_threshold = 3.5

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 20
species_elitism      = 2

[DefaultReproduction]
elitism            = 1
survival_threshold = 0.2
min_species_size   = 1
